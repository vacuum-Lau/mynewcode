给定一个长度为 n 的整数序列，请找出最长的不包含重复的数的连续区间，输出它的长度。
输入格式：
第一行包含整数 n。
第二行包含 n 个整数（均在 0∼10e5 范围内），表示整数序列。

输出格式：
共一行，包含一个整数，表示最长的不包含重复的数的连续区间的长度。
数据范围：  1≤n≤10e5

输入样例：
5
1 2 2 3 5
输出样例：
3
  
  可以观察到朴素的暴力是无法解决问题的（10e5平方TLE）那么可以考虑优化双指针
  题目的意思翻译过来就是一段区间里，每一个数都不重复。
  那么我们可以维护一个数组，统计某一个数的出现次数，当出现重复的时候将这段区间从前面截断。
  也就是说有一个指针i负责向前推进同时记录出现次数，出现重复后有一个指针j负责向前移动截断。
  此时这个区间长度就是i-j+1

  ```cpp
  int main(){
  
  }

