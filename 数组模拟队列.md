# 队列的模拟
## 队列
- 队列两头都可以出，末尾进入，可以用于一些滑动窗口问题的优化
### 数组模拟
- 需要一头一尾，hh=0,tt=-1，队尾加入++tt，弹出--tt，队头弹出++hh
- 因为有了上述操作，hh和tt与数组的真实下标只能说关系不大了，难以判断一些数量问题等等，所以可以视情况引入另一个数组q[]来储存队列中元素对应的数组下标，具体的值就是a[q[]]

### 代码实现
```cpp
#include<iostream>
#include<cstring>
using namespace std;
const int N=100010;
int hh,tt=-1;
int a[N];

int main(){
    int m;
    cin>>m;
    while(m--){
        string s;
        cin>>s;
        if(s=="push"){
            int x;
            scanf("%d",&x);
            a[++tt]=x;
        }
        if(s=="pop"){
            hh++;
        }
        if(s=="empty"){
            if(hh<=tt) printf("NO\n");
            else printf("YES\n");
        }
        if(s=="query"){
            printf("%d\n",a[hh]);
        }
    }
}
