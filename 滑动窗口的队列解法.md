# 滑动窗口：给定一个大小为 n≤10e6 的数组。有一个大小为 k 的滑动窗口，它从数组的最左边移动到最右边。你只能在窗口中看到 k 个数字。每次滑动窗口向右移动一个位置。你的任务是确定滑动窗口位于每个位置时，窗口中的最大值和最小值。
 
## 方法分析：
- 如果暴力，那么需要双指针往前滑，然后每次遍历窗口内部，需要O(nk)极限情况下为n^2/2严重超时
- 思考这个地方也是输出某一种值也就是答案，同样可以想只维护可能为答案的数据
- 类比单调栈，此处多了一个就是容量有限，也就是数据会滑出，所以我们用队列来作为容器
- 这里只分析min其实差不多，右边的数要是更小，那么左边的数永远不能为答案，可以从队列中删除
- 但是这带来一个问题就是对列的下标无法与数组对应也很难计算队内真实数据数量难以判断队头滑动时机，所以引进一个数组专门用于队列下标和数组下标的对应

### 代码实现
```cpp
#include<iostream>
using namespace std;

const int N=1000010;

int hh,tt=-1,a[N],q[N];

int main(){
    int n,k;
    cin>>n>>k;
    
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    
    for(int i=0;i<n;i++){
        if(hh<=tt&&i>q[hh]+k-1) hh++;
        while(hh<=tt&&a[q[tt]]>=a[i]) tt--;
        q[++tt]=i;//注意要修改下标的指向，如果只更改值的话相当于修改a但是tt还是指向原处
        if(i>=k-1) printf("%d ",a[q[hh]]);
    }
    printf("\n");
    hh=0,tt=-1;
    for(int i=0;i<n;i++){
        if(hh<=tt&&i>q[hh]+k-1) hh++;
        while(hh<=tt&&a[q[tt]]<=a[i]) tt--;
        q[++tt]=i;
        if(i>=k-1) printf("%d ",a[q[hh]]);
    }
    return 0;
    
}

