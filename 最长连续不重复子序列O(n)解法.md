# O(n)方法解决最长不重复子序列问题
## 题目描述： 给定一个长度为n的整数序列，找出最长的不包含重复数的连续区间并且输出长度
## 思路分析：
- 可以直接暴力维护一个已经遍历的数组，一边将新的数预期比较看是否重复。但是这个的复杂度应该是O（n^2）以上对于较大数据必定tle
## O（n）解决方案:
- 用一个新数组足够大记录某个数据的出现次数
- 将a数组的数往里面加 [a[i]]++ 同时增加ret
- 若[a[i]]++>1 那么出现了重复 那么从j（这一段子序列的开始）向前遍历，将的数据恢复直到中所有值都小于2（此时的效果是j停在了与a[i]相同的那个点右侧） (此时ret不会被修改) 但是需要注意就是ret的修改要放在判断后面

## 代码实现：
```cpp
#include<iostream>
using namespace std;

const int N=100010;
int a[N],[N];

int main(){
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>a[i];
    }

    int j=0;//初始点
    int ret=0;
    for(int i=0;i<n;i++){
        [a[i]]++;
        while([a[i]]==2){
            [a[j++]]--;
        }

        ret=max(ret,i-j+1);
    }

    cout<<ret;
    return 0;
}
