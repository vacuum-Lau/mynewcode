# 沟槽的matrix多文件误我久矣，今天一劳永逸解决（但愿）

## 基本原理
- 多文件的时候不再是直接编译出一个可运行文件而是形成多个.o（目标文件）然后连接器（linker）将其全部拼接形成一个最终的可执行文件

## 内容
- .h 头文件，属于说明书，写函数声明 宏定义 全局变量等
- .c 具体实现过程

### .h文件
- 头文件要加保护避免重复
  ```c
  #ifndef FUNC_H;
  #define FUNC_H;

  #endif

- 函数写个声明就可以
- 全局变量可以用extern关键词避免报错（只声明不定义，避免重复定义）其实我不是很理解，好像是因为每个文件是单独编译的为了避免这个阶段不报错也不重复，所以用extern。这么看来我还挺了解的

### .c文件
- 要引入需要的头文件，自己写的用“”，内置的用<>

