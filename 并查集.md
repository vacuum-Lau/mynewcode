# 并查集的树模拟一共有 n 个数，编号是 1∼n，最开始每个数各自在一个集合中。现在要进行 m 个操作，操作共有两种：M a b，将编号为 a 和 b 的两个数所在的集合合并，如果两个数已经在同一个集合中，则忽略这个操作；Q a b，询问编号为 a 和 b 的两个数是否在同一个集合中；
## 并查集就是可以合并两个集合以及查询两个元素是否在一个集合里的一种数据结构，为了达到这个目的我们将杂乱无序的集合用树来写。我们规定一个集合（树）有一个根结点，只要能回溯到同一个根结点的元素就是在同一个集合。
## 核心：找爸爸
- 我们设立一个数组p[]用于记录每个元素的父节点，初始化的时候每个元素各自在一个集合中，那么父节点就是自己（这个就是根结点的性质）
- 合并操作：就是要把一个集合的所有元素挂靠到另外一个集合去，但是我们拿到手的只不过两个元素。此处有精妙的操作，直接背下来吧，根本想不到的操作：
  ```cpp
  int find(int x){
    if(p[x]!=x) p[x]=p[find(x)];
    return p[x];
  }
- 代码赏析：思想1 与其每次向上追溯到根结点，不如直接把点接到根结点上，思想2 把一个点挂另外一边去了他屁股后面的固然跟着动了 但是前面的呢，所以要直接把根结点接上去。那么这个代码：如果不是根结点，那么此点的父亲就是父亲的父亲，进入递归
一直到根结点处return p[x]也就是根结点本身，往回每一层p[x]都等于p[p[根结点]]=根结点，所以所有点都挂上了根结点。原本很长一串的树被压扁。
### 完整代码实现：
```cpp
#include<iostream>
using namespace std;

const int N=100010;
int p[N];

int find(int x){
    if(p[x]!=x) p[x]=find(p[x]);
    return p[x];
}
int main(){
    int n,m;
    cin>>n>>m;
    
    for(int i=1;i<=n;i++) p[i]=i;
    
    while(m--){
        char op;
        int a,b;
        scanf(" %c%d%d",&op,&a,&b);
        if(op=='M'){
            p[find(b)]=find(a);
        }
        if(op=='Q'){
            if(find(a)==find(b)) printf("Yes\n");
            else printf("No\n");
        }
    }
    
    
    return 0;
}
