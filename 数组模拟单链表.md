# 用数组模拟链表
## 需要实现的功能
- 向链表头插入一个数
- 删除第k个插入的数的后面的那个数
- 在第k个插入的数后插入一个数

## 关于模拟的方法
两个数组 e[]（存值） 和 ne[] （存储下一个节点的idx）两个指针IDX和hh idx是第几个插入的数(对应数组的下标)，hh（头节点对应的数组下标）
- 头插：e[idx]=x（存值） ne[idx]=hh（下一节点为当前的头节点） hh=idx(更新头节点)idx++（下一个点）
- 删除：首先翻译第k个插入的数即idx=k-1的点（但是注意如果是k==0那么就是要删头节点，方式有所不同）
- 中间插入：e[idx]=x ne[idx]=ne[k-1] (这个地方有没有可能也来一个0？) ne[k-1]=idx idx++

## 完整代码
```c

#include<iostream>
using namespace std;

const int N=100010;
int e[N],ne[N],idx=0,hh=-1;

void addh(int x){
    e[idx]=x;
    ne[idx]=hh;
    hh=idx++;
}

void del(int k){
    ne[k-1]=ne[ne[k-1]];
}

void addk(int k,int x){
    e[idx]=x;
    ne[idx]=ne[k-1];
    ne[k-1]=idx++;
}
int main(){
    int m;
    cin>>m;
    while(m--){
        char c;
        cin>>c;
        if(c=='H'){
            int k;
            cin>>k;
            addh(k);
        }
        
        if(c=='D'){
            int k;
            cin>>k;
            if(k==0) hh=ne[hh];
            else del(k);
        }
        
        if(c=='I'){
            int k,x;
            cin>>k>>x;
            addk(k,x);
        }
    }
    for(int i=hh;i!=-1;i=ne[i]) printf("%d ",e[i]);
    
    
    return 0;
